# Products OOP вЂ” C#

## Description

- `Product` вЂ” abstract base class with static field `Count` tracking total created products
- `ElectronicProduct` вЂ” adds `WarrantyPeriod` field
- `ClothingProduct` вЂ” adds `Size` field
- `PrintInfo()` вЂ” displays product info including total count

## Run

```bash
dotnet run
```
